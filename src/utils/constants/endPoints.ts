/**
 * Not keeping as ENUM, as sometimes urls need to be structured dynamically
 * And this is not supported by ENUMs
 */
export const END_POINTS = {
	classDetails: "userClass/class-details/",
	filterStudents: "user/filter-students",
};
